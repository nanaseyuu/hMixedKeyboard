/node_modules
/oh_modules
/local.properties
/.idea
**/build
/.hvigor
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer
*.hap
*.hsp
*.log

# signing material: private keystore, certs and profiles must never be pushed
/signing/

# local machine script with the real keystore password (see cli_sign_install.example.bat)
/cli_sign_install.bat
/prep_signing.bat

# local-only helper scripts (device addresses, UDIDs, debug probes)
/pad_deploy.bat
/pad_udid.txt
/kb_watch.bat
/analyze_kb.ps1
/dump_check.js
/key_bounds.js
/list_types.js
/node_bounds.js

/build

# stray nested duplicate of the whole project (active module is ./entry)
/hMixedKeyboard/
# local DevEco/CodeArts cache
/.bitfun/

# DevEco signing configs (machine-local encrypted material) stay out of the
# public repo — build-profile.example.json5 is the tracked empty template
build-profile.json5

# transient unsigned-copy of build-profile used by cli_build.bat (contains signing material)
build-profile.local.json5

# local diagnostic / verification artifacts from device testing
.diag/
.verify/
